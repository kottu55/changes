<h1>categries/show</h1>

<p><%= @category.name %></p>

<% @posts.each do |post| %>

    <%= link_to post_path(post.id) do %>
    <%= attachment_image_tag post, :before_img, :fill, 100, 100, format: 'jpeg' %>
    <%= attachment_image_tag post, :after_img, :fill, 100, 100, format: 'jpeg' %>
    <% end %>
    <%= post.user_id %>
    <%= post.title %>
    <%= post.caption %>
    <%= post.time_length %>
    <%= post.category_id %>
    <%= post.owned_tag_id %>

 <% end %>